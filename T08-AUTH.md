# T08 인증 구현 설명서

## ① 무엇으로 붙였나

등록 옵션 생성과 등록·로그인 검증은 **라이브러리 `@simplewebauthn/server` 14.0.2**에 맡겼습니다. 브라우저는 표준 `navigator.credentials.create/get`을 사용합니다. 세션·challenge 보관·소유자 검사·패스키 관리는 직접 작성했습니다. 서버는 Cloudflare Workers, 저장소는 D1입니다. 비밀번호 입력이나 비밀번호 인증은 없습니다.

서버의 `passkeys.public_key`에는 COSE 공개키의 base64url 표현이 저장됩니다. 공개키는 서명 검증용으로, 비밀번호나 개인키가 아닙니다. 실제 저장값은 `evidence/http-evidence.json`의 `/api/passkeys` 응답에서 확인합니다. 등록 본문에는 `clientDataJSON`, `attestationObject`, credential ID, transports가 들어 있고 개인키 필드는 없습니다. 테스트 인증기의 개인키도 증거 파일에 저장하지 않았습니다.

시험에 사용한 저장 위치는 **소프트웨어 테스트 인증기**입니다. 브라우저 검사는 Edge의 CDP 가상 인증기이고, 화면에서 선택한 ‘기기 자체’는 이 검사에서만 가상 기기를 뜻합니다. 제출자 본인의 Google 비밀번호 관리자·기기·보안 키 등록은 `SUBMISSION.md`의 남은 확인 단계에서 별도로 기록해야 합니다. 사용자 기기에 실제 등록했다고 주장하지 않습니다. 동기화형 패스키는 제공자의 암호화 동기화를 지원할 수 있지만, 이 앱 서버에는 개인키를 보내지 않습니다.

## ② 왜 그걸 골랐나

WebAuthn의 challenge·Origin·RP ID·사용자 확인·서명 검증을 검증된 라이브러리에 맡기면서 요청과 저장된 공개키를 설명하기 위해 골랐습니다. 비밀번호 로그인과 외부 OAuth는 패스키만 쓰는 이번 과제와 맞지 않아 넣지 않았습니다. 세션을 D1에 저장해 로그아웃과 패스키 삭제를 즉시 반영합니다. 구현 참고: https://simplewebauthn.dev/docs/packages/server

## ③ 어디를 어떻게 고쳤나

T01 원본 `b99a52fe45ca5e373f999cad459d270c6e9b3061`의 소개·프로젝트·기술·경험·이미지를 유지하고 ‘나만의 기록’ 링크와 영역만 추가했습니다. 원본 `About-me`에는 수정하지 않고 `About_me_new`에서 작업합니다.

| 흐름 | 소스 위치 |
|---|---|
| 등록 | `public/private.js` register → `worker/index.mjs` `/api/register/options` → D1 challenges → `/api/register/verify` → 공개키 저장 |
| 로그인 | private.js 로그인 버튼 → `/api/login/options` → `/api/login/verify` → 저장 공개키로 서명 확인 → D1 sessions |
| 로그아웃 | private.js 로그아웃 → `/api/logout` → 세션·진행 중 challenge 삭제, 쿠키 만료 |
| 비공개 자료 조회 | refresh → `/api/private` → requireSession → notes.owner를 세션 주인으로 제한 |
| 타인 주소 거절 | `/api/accounts/:owner/private` → 요청 owner와 세션 owner 비교 → 403 |
| 패스키 삭제 | `/api/passkeys/:id` DELETE → 최근 5분 인증·소유자·2개 이상 확인 → 삭제와 계정 세션 폐기를 D1 batch로 실행 |

서버가 등록·로그인 challenge를 생성하고 브라우저 흐름 쿠키의 SHA-256과 묶어 2분 동안 D1에 보관합니다. 검증 시 `DELETE … RETURNING`으로 한 번만 가져옵니다. 실패한 시도도 소비하며 취소 시 삭제합니다. 추가 등록에는 로그인과 최근 5분 인증이 필요하고, 서명 검증 후 저장 시 세션 유효성을 다시 검사합니다. 새 계정은 서버 난수 ID로만 생성해 남의 계정 ID를 지정할 수 없습니다.

인증 수단은 **서버 세션**입니다. 256비트 난수의 원문은 HttpOnly·SameSite=Strict·Secure 쿠키에만, DB에는 SHA-256 지문만 저장합니다. 로그인 후 1시간에 만료됩니다. localhost 개발에서만 Secure를 생략합니다. 요청의 URL·본문 owner 값은 내 자료 조회 권한에 쓰지 않습니다. 모든 변경 요청은 정확한 Origin과 JSON 형식을 검사합니다.

## ④ 안 열리는 것을 확인한 기록

`evidence/http-evidence.json`은 **실제로 실행해 단언한 48개 요청·응답**입니다. 로컬 Node SQLite 어댑터 위에서 운영 Worker 처리 코드와 실제 라이브러리 암호 검증을 호출했습니다. 실제 배포 서버 기록이나 사용자의 물리 기기 기록으로 표현하지 않습니다. 성공·거절은 아래 label과 path로 나란히 대조합니다.

| 확인 | 성공 | 거절 |
|---|---|---|
| 로그인 없이 열기 | 계정 A `/api/private` 200, 공개 `/` 200 | anonymous 같은 API 401 |
| 남의 패스키로 열기 | A·B 각자 자료 200 | A→B 및 B→A 지정 주소 각 403 |
| 이미 쓴 질문 재사용 | ‘올바른 서명 성공’ 200 | 같은 요청 본문 ‘사용한 질문 재사용 거절’ 400 |
| 패스키 삭제 뒤 로그인 | ‘남은 패스키 로그인 성공’ 200 | ‘삭제한 패스키 로그인 거절’ 401 |

추가 증거: 틀린 서명·다른 Origin·사용자 확인 없는 서명은 401, challenge 만료 400, 로그아웃 뒤 **같은 이전 쿠키** 재사용 401, 세션 만료 401, 마지막 키 삭제 409, 타인 키 삭제 403입니다. 등록 취소 후 accounts와 passkeys 저장 건수는 0임을 검사했습니다. 등록·로그인 각각 두 요청의 다른 challenge 값이 본문에 있습니다. 두 계정의 가상 항목은 각각 3개이며 공격 전후 전체 응답의 일치를 검사했습니다. URL·본문에 B의 ID를 넣어도 A의 자료만 반환합니다.

`evidence/public-response.html`, `locked-response.html`에는 비공개 항목 본문이 들어 있지 않음을 검사했습니다. 로컬 Cloudflare workerd/D1과 Edge 가상 인증기로 브라우저 검사 10항목도 통과했습니다. `evidence/browser-verification.json`과 두 키·삭제 후 화면 캡처로 확인합니다. 실제 기기 확인은 별도이며 미실행 항목은 통과로 간주하지 않습니다.

## ⑤ AI와 나

- AI에게 맡긴 일: 원본 복사, 패스키·세션·소유자 검사 구현, 테스트 인증기로 요청 실행, 증거와 설명서 초안 정리.
- 내가 직접 판단한 일: T01 공개 소개를 유지하고, 원본 About-me를 절대 수정하지 않으며 별도 About_me_new 저장소에서 확장하도록 정했습니다.
- AI 제안을 따르지 않은 일: 이미 대화에 있던 수업·T07 배포 정보를 다시 묻는 대신 이전 대화에서 찾아 활용하도록 요청했습니다. 불필요한 반복 검사도 줄이도록 했습니다.

기기에서의 본인 확인·실제 저장 위치 선택은 사용자가 합니다. AI가 이를 대신 수행했다고 쓰지 않습니다.

## ⑥ 아직 못 막은 것

- **모든 패스키 분실 복구는 미구현**입니다. 마지막 키의 서버 삭제는 막지만, 기기·동기화 계정·보안 키를 모두 잃으면 기존 자료에 접근할 수 없습니다. 새 가입은 새 계정입니다.
- **분산 봇 가입·요청 남용 방어와 계정 정리 UI가 없습니다.** 요청 본문 크기·키 개수는 제한하지만 자동 가입이 DB 용량과 비용을 소모할 수 있습니다. 실제 민감정보 서비스로 쓰기 전 가입 제한·요청 제한·계정 삭제·관제 보완이 필요합니다.
- **감염된 기기나 탈취된 유효 세션**은 완전히 막지 못합니다. 세션 1시간 만료·HttpOnly 쿠키·키 관리 5분 재인증으로 노출을 줄입니다.
- 저장 위치 이름은 사용자가 선택한 설명이며 서버가 실제 패스키 관리자 이름을 인증한 값이 아닙니다. 브라우저별 물리 기기 호환성 전수 검사는 하지 않았습니다.
